import { CircularProgress } from '@mui/material';
import React from 'react';

const Spiner = () => {
    return (
        <div id='loading'>
            <CircularProgress />
        </div>
    );
}

export default Spiner;
